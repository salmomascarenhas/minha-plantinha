<style>
    @keyframes wiggle {

        0%,
        100% {
            transform: rotate(-3deg);
        }

        50% {
            transform: rotate(3deg);
        }
    }

    .animate-wiggle {
        animation: wiggle 0.6s ease-in-out infinite;
    }

    @keyframes float {

        0%,
        100% {
            transform: translateY(0);
        }

        50% {
            transform: translateY(-6px);
        }
    }

    .animate-float {
        animation: float 3s ease-in-out infinite;
    }

    @keyframes fade-in-up {
        0% {
            opacity: 0;
            transform: translateY(20px);
        }

        100% {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .animate-fade-in-up {
        animation: fade-in-up 1s ease-out forwards;
    }

    @keyframes slide-down {
        from {
            opacity: 0;
            transform: translateY(-20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .animate-slide-down {
        animation: slide-down 0.6s ease-out forwards;
    }
</style>


<div class="bg-slate-50 min-h-screen">
    <!-- Cabeçalho -->
    <header class="fixed inset-x-0 top-0 z-50 bg-white/80 backdrop-blur shadow-sm animate-slide-down">
        <nav class="flex items-center justify-between px-6 py-4 lg:px-8" aria-label="Global">
            <!-- Logo -->
            <div class="flex lg:flex-1">
                <a href="#"
                    class="flex items-center gap-2 transition-transform duration-200 hover:scale-105 hover:drop-shadow-md">
                    <span class="text-2xl animate-wiggle">🌱</span>
                    <span
                        class="text-lg font-extrabold text-slate-800 tracking-wide animate-fade-in-up transition-opacity duration-700">
                        Minha Plantinha
                    </span>
                </a>
            </div>

            <!-- Botão "Entrar" com efeito de preenchimento gamificado funcional -->
            <div class="lg:flex lg:flex-1 lg:justify-end">
                <a href="/login"
                    class="group relative inline-block px-4 py-2 text-sm font-semibold border border-slate-300 rounded-md overflow-hidden transition-colors duration-300">

                    <!-- Fundo animado -->
                    <span
                        class="absolute inset-0 left-0 top-0 bg-green-500 scale-x-0 group-hover:scale-x-100 origin-left transition-transform duration-300 ease-out transform"></span>

                    <!-- Texto acima da barra -->
                    <span class="relative z-10 text-slate-900 group-hover:text-white transition-colors duration-300">
                        Entrar <span aria-hidden="true">&rarr;</span>
                    </span>
                </a>
            </div>

        </nav>
    </header>

  <script>
  const header = document.getElementById('main-header');

  window.addEventListener('scroll', () => {
    if (window.scrollY > 0) {
      header.classList.add('shadow-md');
    } else {
      header.classList.remove('shadow-md');
    }
  });
</script>

  <script>
  const header = document.getElementById('main-header');

  window.addEventListener('scroll', () => {
    if (window.scrollY > 0) {
      header.classList.add('shadow-md');
    } else {
      header.classList.remove('shadow-md');
    }
  });
</script>

    <section class="relative isolate px-6 pt-24 lg:px-8">
        <div class="mx-auto max-w-2xl py-24 sm:py-36 lg:py-44 animate-fade-in-up">
            <div class="text-center">
                <h1 class="text-4xl font-extrabold tracking-tight text-slate-900 sm:text-6xl drop-shadow">
                    Cuide da sua planta de um jeito novo e <span class="text-green-600">divertido</span>.
                </h1>
                <p class="mt-6 text-lg leading-8 text-slate-600">
                    Monitore, controle e interaja com sua planta com uma experiência gamificada e cheia de surpresas. 🌼
                </p>
                <div class="mt-10 flex flex-col sm:flex-row justify-center items-center gap-4">
                    <a href="/register"
                        class="rounded-lg bg-green-600 px-6 py-3 text-sm font-semibold text-white shadow-lg hover:bg-green-500 hover:shadow-xl transform hover:scale-105 transition-all animate-float">
                        🌟 Criar Conta Grátis
                    </a>
                    <a href="#features"
                        class="text-sm font-semibold leading-6 text-slate-900 hover:text-green-700 hover:underline transition">
                        Saber mais <span aria-hidden="true">→</span>
                    </a>
                <h1 class="text-4xl font-bold tracking-tight text-slate-900 sm:text-6xl">Cuide da sua planta de um jeito novo e divertido.</h1>
                <p class="mt-6 text-lg leading-8 text-slate-600">Monitore, controle e interaja com sua planta através de uma experiência gamificada, transformando a jardinagem em uma aventura diária.</p>
                <div class="mt-10 flex items-center justify-center gap-x-6">
                    <a href="/register" class="rounded-md bg-green-600 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-green-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-green-600">Criar Conta Grátis</a>
                    <a href="/login" class="max-w-xs text-center rounded-md border border-green-600 bg-white px-3.5 py-2.5 text-sm font-semibold text-green-600 hover:bg-green-50 hover:border-green-800 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-green-600">Já tenho conta</a>
                    <a href="/saibaMais" class="text-sm font-semibold leading-6 text-slate-900">Saber mais <span aria-hidden="true">→</span></a>
                </div>
            </div>
        </div>
    </section>


    <!-- Seção de Recursos -->
    <section id="features" class="bg-white py-24 sm:py-32">
        <div class="mx-auto max-w-7xl px-6 lg:px-8">
            <div class="mx-auto max-w-2xl text-center animate-fade-in-up">
                <h2 class="text-base font-semibold text-green-600 uppercase">Tudo o que você precisa</h2>
                <p class="mt-2 text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
                    Uma plataforma completa para o jardineiro moderno
                </p>
            </div>

            <div class="mt-16 grid max-w-4xl mx-auto grid-cols-1 gap-12 sm:grid-cols-2">
                <!-- Card 1 -->
                <div class="relative pl-16 transition-transform hover:-translate-y-1 hover:shadow-md duration-200">
                    <dt class="text-base font-semibold text-gray-900">
                        <div
                            class="absolute left-0 top-0 flex h-10 w-10 items-center justify-center rounded-lg bg-green-600 shadow-md">
                            <span class="text-white text-xl animate-wiggle">📊</span>
                        </div>
                        Monitoramento em Tempo Real
                    </dt>
                    <dd class="mt-2 text-base text-gray-600">
                        Acompanhe umidade, temperatura e luz no seu painel com atualizações constantes.
                    </dd>
                </div>

                <!-- Card 2 -->
                <div class="relative pl-16 transition-transform hover:-translate-y-1 hover:shadow-md duration-200">
                    <dt class="text-base font-semibold text-gray-900">
                        <div
                            class="absolute left-0 top-0 flex h-10 w-10 items-center justify-center rounded-lg bg-green-600 shadow-md">
                            <span class="text-white text-xl animate-bounce">🎮</span>
                        </div>
                        Gamificação e Mascote Virtual
                    </dt>
                    <dd class="mt-2 text-base text-gray-600">
                        Veja o mascote reagir ao estado da planta, ganhe pontos por cuidar bem dela e desbloqueie
                        conquistas!
                    </dd>
                </div>
            </div>
        </div>
    </section>

</div>